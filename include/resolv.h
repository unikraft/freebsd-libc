/*
 * Minimal <resolv.h> overlay. The only consumer (inet/nsap_addr.c) includes it
 * but uses nothing from it, so this is an empty placeholder rather than the
 * full BIND resolver header.
 */
#ifndef _LIBFREEBSD_RESOLV_H_
#define _LIBFREEBSD_RESOLV_H_

#endif /* _LIBFREEBSD_RESOLV_H_ */
